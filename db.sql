CREATE TABLE propiedades (
    nombre TEXT PRIMARY KEY UNIQUE,
    descripcion    TEXT,
    precio INTEGER,
    rec INTEGER,
    b INTEGER,
    bc INTEGER,
    mb INTEGER,
    est INTEGER,
    ter INTEGER,
    const INTEGER,
    direccion TEXT,
    ubi TEXT
)

INSERT 
